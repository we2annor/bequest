"use strict";

var _toolkit = require("@reduxjs/toolkit");

var initialState = {
  addresses: []
};
var address = (0, _toolkit.createSlice)({
  name: "address",
  initialState: initialState,
  reducers: {}
});